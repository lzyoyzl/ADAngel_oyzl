name: adangel-sm120
channels:
  - defaults
dependencies:
  # Keep Python independent of the host Ubuntu Python. The remaining packages are
  # installed with pip so that PyTorch can use its official cu128 wheel index.
  - python=3.10.12
  - pip
