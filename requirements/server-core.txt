# Install torch from the official cu128 index as documented in docs/miniconda_setup.md.
# Use --no-deps for the editable project install after these requirements are present.
PyYAML==6.0.2
torch==2.7.1
typing_extensions==4.12.2
